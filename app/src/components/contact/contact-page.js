import React, { Component } from 'react';

import { Map } from './map';

export default class Contact extends Component {
    render(){
        return (
            <div>
                <Map/>
            </div>
        )
    }
}